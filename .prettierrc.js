module.exports = {
  // Print width: 120 characters (matches ESLint max-len)
  printWidth: 120,

  // Tab width: 2 spaces
  tabWidth: 2,

  // Use spaces instead of tabs
  useTabs: false,

  // Semicolons at end of statements
  semi: true,

  // Use single quotes
  singleQuote: true,

  // Trailing commas: ES5 (objects, arrays)
  trailingComma: 'es5',

  // Spaces inside object literals: { foo: bar }
  bracketSpacing: true,

  // Arrow function parentheses: always
  arrowParens: 'always',

  // End of line: auto (let git handle it)
  endOfLine: 'auto',

  // Wrap prose (markdown, etc.) at printWidth
  proseWrap: 'preserve',
};
